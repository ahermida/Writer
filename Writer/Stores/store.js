/**
 * Data Storage for App
 */
import w from '../w'
var store                  = {};
store.frontPage            = {};
store.frontPage.targetItem = "";
store.frontPage.inputNote  = false;

export default store;
